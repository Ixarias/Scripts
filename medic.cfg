// Medic Scripting

exec binds
exec cleanup
exec settings

// Medic Music
// Plays "MEDIC!" on first class load.
medic_music

// Medi-Gun
// Starts the Medic with his medi-gun active.
slot2

// +Attack Reverse
bind 2 "heal_reverse"
alias heal_reverse "bind mouse1 +heal; slot2; -heal"
heal_reverse

alias +heal "-attack"
alias -heal "+attack"
-heal

bind 1 "gun_standard"
bind 3 "melee_standard"

alias gun_standard "-attack; bind mouse1 +attack; slot1'
alias melee_standard "-attack; bind mouse1 +attack; slot3
